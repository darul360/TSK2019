using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SpawnSatelite : MonoBehaviour
{
    public GameObject sateliteDonor;
    private GameObject tempObject;
    // Start is called before the first frame update
    void Spawn()
    {
        //GameObject receiver = GameObject.FindGameObjectWithTag("clone");
        //tempObject = Instantiate(sateliteDonor, dir, Quaternion.identity);
        // float distanceX = Mathf.Sqrt(
    }
}
